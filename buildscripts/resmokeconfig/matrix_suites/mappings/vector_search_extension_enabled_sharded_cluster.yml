description: Runs mongot e2e sharded cluster tests (sharding_no_passthrough) with the $vectorSearch extension loaded and enabled via the featureFlagVectorSearchExtension IFR flag.

base_suite: mongot_e2e_sharded_cluster
overrides:
  - vector_search_extension.exclude_sharded_cluster_incompatible_tests
  - vector_search_extension.load_extension_sharded
  - vector_search_extension.extension_enabled_sharded_non_community
