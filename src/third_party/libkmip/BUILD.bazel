load("//bazel:mongo_src_rules.bzl", "mongo_cc_library")

package(default_visibility = ["//visibility:public"])

mongo_cc_library(
    name = "libkmip",
    srcs = glob(["dist/src/*.c"]),
    hdrs = glob(["dist/include/*.h"]),
    includes = ["dist/include"],
    no_undefined_ref_DO_NOT_USE = False,
)
